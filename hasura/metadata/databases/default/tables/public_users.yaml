table:
  name: users
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - activity
        - auth_id
        - created_at
        - id
        - preferences
        - updated_at
      filter:
        auth_id:
          _eq: X-Hasura-User-Id
update_permissions:
  - role: user
    permission:
      columns:
        - activity
        - preferences
      filter:
        auth_id:
          _eq: X-Hasura-User-Id
      check: null
